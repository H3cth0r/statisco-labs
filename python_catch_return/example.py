# Function will define input params and will declare return value
# catch_return will then use the name of the funcion and parameters
# to then mofidy the return variable. The variables should execute
# C function with the same name and then return the result
@catch_return
def sum(a, b): return res
